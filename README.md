# best-coffe-shop
Temporal.io tutorial in python


#### Tech Stack :
    docker, 
    docker-compose, 
    python, 
    temporal, 
    SvelteJS - Web UI

#### Setup
Run `docker-compose up` to create temporal container